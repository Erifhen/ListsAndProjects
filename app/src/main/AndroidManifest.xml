<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.erickramonfcastro.listsandprojects">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListsAndProjects">
        <activity
            android:name=".AddProjectActivity"
            android:exported="false" />
        <activity
            android:name=".RoutinesActivity"
            android:exported="false"
            android:label="@string/title_activity_routines"
            android:theme="@style/Theme.ListsAndProjects.NoActionBar" />
        <activity
            android:name=".Projects"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>